#if 0
* born.F
* form factors for {-F[3, {1, Col1}], F[3, {1, Col2}]} -> {V[1], V[5, {Glu4}]}
* generated by FormCalc 9.4 (7 Jun 2016) on 15-Dec-2016 16:37
#endif

#include "vars.h"
#include "inline.h"
#include "contains.h"


	subroutine born
	implicit none

#include "vars.h"
#include "inline.h"

        Ctree(HelAll(1)) = 
     &   Ctree(HelAll(1)) + 
     &    2/3.D0*(EL*GS*(Sub2/(MU2 - T) + 
     &         (F3 + F5 - 2*Sub1)/(MU2 - U)))

#if DEBUG >= 4
	DEB("born:Ctree(1)", Ctree(HelAll(1)))
#endif

#include "contains.h"

	end

